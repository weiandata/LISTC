{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/weiandata/LISTR/inst/schema/config.schema.json",
  "title": "LISTR run configuration",
  "type": "object",
  "required": ["data", "roles", "tables"],
  "properties": {
    "data": {
      "type": "string",
      "description": "Path to csv/tsv/xlsx/sav/dta/sas7bdat data file"
    },
    "roles": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": { "type": "string" },
        "weight": { "type": "string" },
        "group": { "type": "array", "items": { "type": "string" } },
        "score": { "type": "array", "items": { "type": "string" } },
        "theta": { "type": "object", "additionalProperties": { "type": "string" } },
        "theta_se": { "type": "object", "additionalProperties": { "type": "string" } },
        "resp": { "type": "array", "items": { "type": "string" } },
        "key": { "type": "object", "additionalProperties": true },
        "rep_weights": {
          "description": "Replicate weight columns: array of names, or a single prefix string like W_FSTR (expands to W_FSTR1..R)",
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ]
        },
        "rep_method": { "enum": ["fay", "brr", "jk1", "jk2"] },
        "fay_k": { "type": "number", "exclusiveMinimum": 0, "exclusiveMaximum": 1 },
        "pv": {
          "description": "Plausible values: dimension -> array of columns or a template with # for the PV number, e.g. {math: \"PV#MATH\"}",
          "type": "object",
          "additionalProperties": {
            "oneOf": [
              { "type": "string" },
              { "type": "array", "items": { "type": "string" } }
            ]
          }
        },
        "pv_sampling": { "enum": ["first", "average"] }
      }
    },
    "tables": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "values"],
        "properties": {
          "name": { "type": "string" },
          "rows": { "type": "array", "items": { "type": "string" } },
          "cols": { "type": "array", "items": { "type": "string" } },
          "values": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "required": ["stat"],
              "properties": {
                "stat": {
                  "enum": ["st_mean", "st_prop_above", "st_level_prop",
                           "st_quantile", "st_sd", "st_count", "st_wcount",
                           "st_pvalue", "st_option_dist"]
                },
                "var": { "type": "string" },
                "cutoff": { "type": "number" },
                "breaks": { "type": "object", "additionalProperties": { "type": "number" } },
                "probs": { "type": "array", "items": { "type": "number" } },
                "method": { "enum": ["hard", "prob"] },
                "correction": { "enum": ["none", "latent"] },
                "rho": { "type": "number" }
              }
            }
          },
          "format": { "enum": ["est", "est_se", "est_ci", "percent"] },
          "digits": { "type": "integer" },
          "margins": { "type": "boolean" }
        }
      }
    },
    "output": {
      "type": "object",
      "properties": {
        "xlsx": { "type": "string" },
        "json": { "type": "string" },
        "html": { "type": "string" }
      }
    }
  }
}
