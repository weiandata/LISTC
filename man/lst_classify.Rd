% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{lst_classify}
\alias{lst_classify}
\title{Classify a variable into proficiency levels by cut scores}
\usage{
lst_classify(x, var, breaks, labels = NULL, name = NULL)
}
\arguments{
\item{x}{A \code{listr_data} object.}

\item{var}{Measure: theta/score dimension name or numeric column.}

\item{breaks}{Named numeric vector: level name -> lower bound,
e.g. \code{c(不及格 = -Inf, 及格 = 0.5, 优秀 = 1.2)}.}

\item{labels}{Optional level labels (defaults to names of \code{breaks}).}

\item{name}{Name of the new column (default \verb{<var>_level}).}
}
\value{
\code{x} with an ordered-factor level column added.
}
\description{
Boundary convention: reaching a cut score places the person in the
higher level (\code{x >= lower & x < upper}).
}
\examples{
d <- data.frame(id = 1:100, region = rep(c("north", "south"), 50),
                w = runif(100, 0.5, 2), theta = rnorm(100),
                se = runif(100, 0.2, 0.4))
x <- lst_data(d, id = id, group = region, weight = w,
              theta = c(math = theta), theta_se = c(math = se))
x <- lst_classify(x, math, c(low = -Inf, mid = -0.5, high = 0.8))
table(x$data$math_level)
}
