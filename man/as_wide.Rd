% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot.R
\name{as_wide}
\alias{as_wide}
\title{Extract the wide (row x column layout) form of a listc_table}
\usage{
as_wide(tab)
}
\arguments{
\item{tab}{A \code{listc_table}.}
}
\value{
A tibble laid out as declared in \code{\link[=lst_table]{lst_table()}}.
}
\description{
Cells are formatted according to the table's \code{format} and \code{digits};
proportion-type statistics are shown as percentages.
}
\examples{
d <- data.frame(id = 1:100, region = rep(c("north", "south"), 50),
                w = runif(100, 0.5, 2), theta = rnorm(100),
                se = runif(100, 0.2, 0.4))
x <- lst_data(d, id = id, group = region, weight = w,
              theta = c(math = theta), theta_se = c(math = se))
tab <- lst_table(x, rows = region, values = list(mean = st_mean(math)))
as_wide(tab)
}
