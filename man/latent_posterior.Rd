% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{latent_posterior}
\alias{latent_posterior}
\title{Empirical-Bayes posterior transform for WLE/ML estimates}
\usage{
latent_posterior(theta, se, w, rho = NULL)
}
\arguments{
\item{theta}{WLE/ML ability estimates.}

\item{se}{Individual sampling standard errors.}

\item{w}{Weights.}

\item{rho}{Optional reliability; estimated from data when \code{NULL}.}
}
\value{
List with posterior \code{theta}, \code{se}, and the \code{rho} used.
}
\description{
\code{correction = "latent"} (design doc 4.1). Probabilistic classification
(\code{method = "prob"}) is calibrated when \code{theta} is an EAP estimate and
\code{se} its posterior SD - no correction needed (verified by simulation).
For unbiased WLE/ML estimates with sampling SEs, the naive normal
probability overstates the spread of the latent distribution; this
transform shrinks each estimate to its normal-model posterior first:
reliability \code{rho = 1 - mean_w(se^2)/var_w(theta)}, then
\verb{theta* = mu + rho*(theta - mu)} and \verb{se* = sqrt(rho)*se}.
}
\keyword{internal}
