% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-html.R
\name{lst_to_html}
\alias{lst_to_html}
\title{Export listc_table(s) as a standalone styled HTML report}
\usage{
lst_to_html(tab, path = NULL, title = NULL, interpret = TRUE)
}
\arguments{
\item{tab}{A \code{listc_table} or named list of them.}

\item{path}{Optional output .html path; when \code{NULL}, returns the HTML
string invisibly.}

\item{title}{Report title; \code{NULL} uses a built-in default title
(Chinese for "statistical results").}

\item{interpret}{Include the rule-based interpretation section.}
}
\value{
HTML string, invisibly.
}
\description{
Dependency-free HTML rendering (v0.4): Chinese-friendly fonts,
zebra-striped pivot tables, an interpretation section per table and a
methods footnote describing the variance engine in use. Suitable for
emailing or embedding in Rmd/Quarto via \code{htmltools::HTML}.
}
\examples{
d <- data.frame(id = 1:100, region = rep(c("north", "south"), 50),
                w = runif(100, 0.5, 2), theta = rnorm(100),
                se = runif(100, 0.2, 0.4))
x <- lst_data(d, id = id, group = region, weight = w,
              theta = c(math = theta), theta_se = c(math = se))
tab <- lst_table(x, rows = region, values = list(mean = st_mean(math)))
f <- tempfile(fileext = ".html")
lst_to_html(tab, f, title = "Report")
file.exists(f)
}
