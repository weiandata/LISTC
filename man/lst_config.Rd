% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{lst_config}
\alias{lst_config}
\title{Read and validate a LISTC run configuration}
\usage{
lst_config(config)
}
\arguments{
\item{config}{Path to .yml/.yaml/.json, a YAML/JSON string, or a list.}
}
\value{
A validated \code{listc_config} object.
}
\description{
Accepts a YAML/JSON file path, a YAML/JSON string, or a named list.
Validation errors are reported in plain Chinese pointing to the
offending field. (Excel configuration workbooks arrive in v0.2.)
}
\examples{
cfg <- lst_config(list(
  data = "students.csv",
  roles = list(id = "id", weight = "w"),
  tables = list(list(name = "t1",
                     values = list(n = list(stat = "st_count"))))
))
class(cfg)
}
