% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{lst_data}
\alias{lst_data}
\title{Create a listr_data object}
\usage{
lst_data(
  data,
  id = NULL,
  group = NULL,
  weight = NULL,
  score = NULL,
  theta = NULL,
  theta_se = NULL,
  resp = NULL,
  key = NULL,
  rep_weights = NULL,
  rep_method = NULL,
  fay_k = 0.5,
  pv = NULL,
  pv_sampling = c("first", "average")
)
}
\arguments{
\item{data}{A data.frame.}

\item{id}{Sample identifier column.}

\item{group}{Demographic/grouping columns.}

\item{weight}{Sampling weight column (defaults to 1 for all rows).}

\item{score}{Observed score column(s).}

\item{theta}{Ability estimate column(s), named by dimension.}

\item{theta_se}{IRT standard error column(s), paired with \code{theta}.}

\item{resp}{Item response columns.}

\item{key}{Optional scoring key for \code{resp} (named vector: item -> correct
answer), required for \code{\link[=st_pvalue]{st_pvalue()}}.}

\item{rep_weights}{Replicate weight columns (v0.3): a character vector
of column names, bare names, or a single prefix string such as
\code{"W_FSTR"} which expands to \code{W_FSTR1..R} (PISA style). When set,
sampling variance switches to the replicate-weights engine.}

\item{rep_method}{Replicate method: \code{"fay"} (PISA BRR-Fay),
\code{"brr"}, \code{"jk1"}, or \code{"jk2"} (TIMSS). Required with \code{rep_weights}.}

\item{fay_k}{Fay factor for \code{rep_method = "fay"} (default 0.5).}

\item{pv}{Plausible values (v0.4): named list, dimension -> PV columns
or a template with \verb{#} for the PV number, e.g.
\code{pv = list(math = "PV#MATH")} expands to PV1MATH..PV10MATH.
PV dimensions are analyzed with Rubin combination.}

\item{pv_sampling}{Sampling-variance convention for PV dimensions:
\code{"first"} (PISA manual practice: first PV only) or \code{"average"}
(average across all PVs).}
}
\value{
A \code{listr_data} object.
}
\description{
Attaches variable roles (id, group, weight, score, theta, theta_se, resp)
to a data frame. Columns can be given as bare names or character
vectors; theta/theta_se accept named vectors defining dimensions,
e.g. \code{theta = c(math = th_math)}.
}
\examples{
d <- data.frame(id = 1:100, region = rep(c("north", "south"), 50),
                w = runif(100, 0.5, 2), theta = rnorm(100),
                se = runif(100, 0.2, 0.4))
x <- lst_data(d, id = id, group = region, weight = w,
              theta = c(math = theta), theta_se = c(math = se))
x
}
